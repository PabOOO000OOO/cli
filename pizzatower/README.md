Предметная область:
Пиццерия

Сущности:
-Товары (пицца, напитки, десерты) - название, описание, цена, категория
-Категории товаров (пицца, напитки, десерты)
-Пользователи - регистрация, авторизация, роли
-Роли пользователей (администратор, пользователь)
-Корзина покупок - связь пользователя с товарами и количеством

Структура папок
lib/
├── pizzatower.dart # экспорт всех модулей
├── src/
│ ├── cli/ # Пользовательский интерфейс
│ │ ├── menu.dart # Главное меню и навигация
│ │ └── input_helper.dart # Ввод с валидацией
│ ├── data/ # Слой данных
│ │ ├── database.dart # SQLite, создание таблиц, начальные данные
│ │ └── repositories/ # CRUD-операции для каждой сущности
│ │ ├── tovar_repository.dart
│ │ ├── user_repository.dart
│ │ ├── category_repository.dart
│ │ ├── role_repository.dart
│ │ └── cart_repository.dart
│ └── domain/ # Доменный слой (бизнес-логика)
│ ├── models/ # Модели данных с toMap/fromMap
│ │ ├── tovars.dart
│ │ ├── users.dart
│ │ ├── categories.dart
│ │ ├── roles.dart
│ │ ├── cart.dart
│ │ └── id.dart
│ └── validators/ # Правила валидации
│ └── validators.dart




Что вынесено в каждый слой и почему

1 Domain (доменный слой)
-Модели данных - чистые Dart-классы без зависимостей от БД или ввода/вывода
-Валидаторы - независимые правила проверки данных
-Почему: позволяет легко менять БД или интерфейс, не затрагивая бизнес-логикy

2 Data (слой данных)
-Databasе - работа с SQLite, создание таблиц, внешние ключи
-Repositories - CRUD-операции, преобразование map ↔ model
-Почему: изолирует работу с БД, позволяет заменить SQLite на JSON или другую БД

3 CLI (интерфейс)
-Menu - навигация, вызов репозиториев
-Input helper - повторный ввод с валидацией
-Почему: отделяет пользовательский интерфейс от бизнес-логики



Валидации

Реализовано 2 типа валидации:

1 Обязательное текстовое поле (не пустая строка после trim) validateNotEmpty()
На что: Имя пользователя, название товара, телефон и т.д.

2 Числовое поле > 0 validatePositiveInt(), validatePositiveDouble()
На что: Цена товара (double > 0) и Количество в корзине (int > 0)



Перечень тестов

Тест CRUD для товаров (pizzatower_test.dart)
-Create - добавление товара
-Read - чтение списка и по ID
-Update - обновление цены и названия
-Delete - удаление товара

Запуск тестов:
dart test
